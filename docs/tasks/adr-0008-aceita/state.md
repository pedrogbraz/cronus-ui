# Retomada

Estado: implementado, aguardando revisão e `full`.

## O que foi feito

Uma linha: `Status: proposta` vira `Status: aceita` em
`docs/adr/0008-marcas-em-blocos.md`.

## Decisões

**A implementação fica fora.** Migrar os três blocos, regenerar baselines e
subir o `lucide-react` são tarefas próprias. A ADR 0002 é o precedente: está
`aceita` com o gate ainda desligado. `aceita` marca a decisão, não a entrega.

**Sem nota de follow-up ainda.** A 0002 só ganhou a dela quando houve o que
relatar.

## Evidências

- `quick`: passed.
- `Data` do cabeçalho segue 2026-09-17, que é a data da escrita e da aceitação.

## Próximo passo

Revisão, `full`, `integrate`. Depois, a migração dos blocos.
