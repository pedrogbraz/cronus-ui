"use client";

import ChartsExamples from "../../lib/examples/charts";

/** Live catalog of every ChartContainer recipe. Client-only because recharts is. */
export function ChartsGallery() {
  return <ChartsExamples slug="chart" />;
}
