# Canonical OSS origin (docs, registry, live template previews).
# `next dev` falls back to http://localhost:4747; production is https://aicronus.com.
# NEXT_PUBLIC_OSS_URL=https://aicronus.com

# This site. `next dev` falls back to http://localhost:4748; production is
# https://iacronus.com.
# NEXT_PUBLIC_PRO_URL=https://iacronus.com
