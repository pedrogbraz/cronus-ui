# Public origin of the OSS docs site (live pack previews + Open source link).
# `next dev` falls back to http://localhost:4747 / :4748 when unset.
NEXT_PUBLIC_OSS_URL=http://localhost:4747
NEXT_PUBLIC_PRO_URL=http://localhost:4748
