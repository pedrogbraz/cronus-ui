# Dependabot — GitHub Actions only.
# npm is lockstep across the published packages; weekly PRs per workspace
# folder were opening preview deploys on Vercel for every bump.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
